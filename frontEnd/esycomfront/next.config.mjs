/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.prod.website-files.com','localhost'], // Add the external domain here
    },
  };
  
  export default nextConfig;