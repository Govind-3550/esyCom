/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.prod.website-files.com','localhost','esycom.onrender.com'], // Add the external domain here
    },
  };
  
  export default nextConfig;